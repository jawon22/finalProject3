<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="attend">
	
	<!-- C 등록 -->
	<insert id="save">
		insert into attend(
			emp_id, attend_no, attend_start, attend_end
			) values(#{empId}, attend_seq.nextval, sysdate, null
		)
	</insert>
	
	<!-- R 목록, 상세 -->
	<!-- 사번별로 근태 조회 -->
	<select id ="findByEmpId" resultType="AttendDto">
		select * from attend where emp_id = #{empId} 
		order by attend_no desc
	</select>
	
	<!-- U 수정 -->
	<!-- 퇴근 버튼 눌렀을 때 -->
	<update id="update">
		update attend set attend_end = sysdate where emp_id = #{empId}
	</update>
	
</mapper>